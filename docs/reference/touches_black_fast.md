# Check if Position is on Black Pixel (Fast)

Fast version that avoids function call overhead.

## Usage

``` r
touches_black_fast(pos, grid)
```

## Arguments

- pos:

  Integer vector of length 2 (row, col).

- grid:

  Numeric matrix. The simulation grid.

## Value

Logical. TRUE if position is on a black pixel.
