% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{validate_termination_position}
\alias{validate_termination_position}
\title{Validate Termination Position}
\usage{
validate_termination_position(pos, grid, neighborhood = "4-hood")
}
\arguments{
\item{pos}{Integer vector of length 2 (row, col). Position to validate.}

\item{grid}{Numeric matrix. Current grid state.}

\item{neighborhood}{Character. "4-hood" or "8-hood". Default "4-hood".}
}
\value{
Logical. TRUE if position is valid for termination, FALSE otherwise.
}
\description{
Checks if a position is valid for termination (has at least one black neighbor
or is the initial center pixel). Used in async mode to prevent isolated pixels
when workers operate on stale grid snapshots.
}
\details{
A termination position is valid if:
\enumerate{
\item The position itself is already black (touching black), OR
\item The position has at least one black neighbor
}

This prevents the creation of isolated black pixels in async mode where
workers may have stale grid snapshots.
}
\examples{
grid <- initialize_grid(10)
# Center (5,5) is black, so (5,6) has a black neighbor
validate_termination_position(c(5, 6), grid, "4-hood")  # TRUE
# Position (1,1) is far from center - no black neighbors
validate_termination_position(c(1, 1), grid, "4-hood")  # FALSE

}
\seealso{
\code{\link{validate_no_isolated_pixels}}
}
