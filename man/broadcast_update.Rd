% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async_controller.R
\name{broadcast_update}
\alias{broadcast_update}
\title{Broadcast Grid Update to Workers}
\usage{
broadcast_update(socket, position, version)
}
\arguments{
\item{socket}{A nanonext publisher socket created by \code{create_pub_socket()}.}

\item{position}{Integer vector of length 2. Grid coordinates \code{c(row, col)}
of the newly black pixel.}

\item{version}{Integer. Current version number of the grid state.
Workers use this to detect stale caches.}
}
\value{
NULL (invisibly). Message is sent asynchronously.
}
\description{
Sends a grid state update message to all subscribed workers via
the nanonext publisher socket.
}
\details{
Broadcasts a pixel update message containing:
\itemize{
\item \code{type}: Always "pixel_update"
\item \code{position}: Coordinates of new black pixel
\item \code{version}: Grid state version number
}

Workers listening on the subscriber socket will receive this message
and update their local caches accordingly.

This is a non-blocking operation - the function returns immediately
after queuing the message for transmission.
}
\examples{
\dontrun{
# Broadcast update when walker terminates
broadcast_update(
  socket = pub_socket,
  position = c(10, 15),
  version = 42
)
}

}
\seealso{
\code{\link{create_pub_socket}}
}
