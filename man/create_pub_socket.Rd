% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async_controller.R
\name{create_pub_socket}
\alias{create_pub_socket}
\title{Create Nanonext Publisher Socket}
\usage{
create_pub_socket(port = 5555)
}
\arguments{
\item{port}{Integer. TCP port for the publisher socket (default: 5555).
Must be available and not blocked by firewall.}
}
\value{
A nanonext socket object configured for publishing.
}
\description{
Creates a nanonext publisher socket for broadcasting grid state updates
to all worker processes.
}
\details{
The publisher socket uses TCP on localhost to broadcast grid updates.
Workers subscribe to this socket to receive notifications when pixels
are added to the black set (walker termination events).

Communication pattern:
\itemize{
\item Main process publishes updates via this socket
\item Worker processes subscribe (see \code{worker_init()})
\item Non-blocking: workers poll for updates between steps
}

The socket binds to \code{tcp://127.0.0.1:<port>} and allows multiple
subscribers to connect.
}
\examples{
\dontrun{
# Create publisher socket
pub_socket <- create_pub_socket(port = 5555)

# Broadcast an update
broadcast_update(pub_socket, position = c(10, 15), version = 42)

# Clean up
nanonext::close(pub_socket)
}

}
\seealso{
\code{\link{broadcast_update}}, \code{\link{cleanup_async}}
}
